// Identifier aliases for the domain. Kept as plain string aliases (not branded)
// to stay ergonomic in seed data and tests; semantic meaning is documented here.

export type EventId = string;
export type ParticipantId = string; // synthetic participant, e.g. "U001"
export type DisclosureId = string;
export type SectionId = string;
export type ReviewId = string;
export type FollowUpId = string;
export type AcknowledgementId = string;
